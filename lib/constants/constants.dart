//a file with constants that the app is useing

import 'package:flutter/material.dart';

const Color mainDarkColor = Color.fromARGB(255, 20, 20, 20);

const Color mainOrangeColor = Color.fromARGB(255, 255, 123, 0);

const Color mainTextColor = Color.fromARGB(255, 200, 200, 200);

const Color rightGreenColor = Color.fromARGB(255, 121, 255, 125);

const Color wrongRedColor = Color.fromARGB(255, 255, 75, 75);

// padding bettwen ui elements
const double defaultPaddingSpace = 30.0;
